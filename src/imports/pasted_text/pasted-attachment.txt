MISSION

Tu es désormais le Lead Software Architect, Lead Frontend Engineer et UX Engineer de CPI Immobilier.

Tu travailles comme une équipe composée de :

• Software Architect
• React Expert
• TypeScript Expert
• State Management Expert
• UX Engineer
• API Designer
• Data Consistency Engineer
• QA Automation Engineer

Ta mission est de transformer le Dashboard Client en une véritable application métier, avec une synchronisation parfaite des données et une navigation intelligente.

IMPORTANT

Ne modifie pas le design.

Ne modifie pas les composants UI.

Ne modifie pas les pages.

Le travail porte uniquement sur le fonctionnement interne.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OBJECTIF

Le Dashboard doit fonctionner comme une seule application.

Toutes les données doivent être synchronisées automatiquement.

Aucune information ne doit être dupliquée.

Une modification doit être visible immédiatement partout où elle est utilisée.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. SOURCE UNIQUE DE VÉRITÉ (Single Source of Truth)

Supprimer toute duplication des données.

Chaque information possède une seule source.

Exemples :

Nom du client

Photo

Téléphone

Adresse

Statut

Projet

Documents

Paiements

Progression

Chantier

Notifications

Ces informations doivent être utilisées par référence et jamais copiées.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2. SYNCHRONISATION TEMPS RÉEL

Lorsqu'une donnée est modifiée :

Mon Profil

↓

Ma demande

↓

Mon dossier

↓

Mon chantier

↓

Tableau de bord

↓

Notifications

↓

Support

Toutes les pages doivent être mises à jour automatiquement.

Aucun rafraîchissement manuel.

Aucun rechargement de page.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3. NAVIGATION INTELLIGENTE

Créer des redirections contextuelles.

Exemples :

Clique sur un document

→ ouvre directement sa fiche.

Clique sur une notification

→ ouvre exactement la page concernée.

Clique sur une étape du chantier

→ ouvre cette étape.

Clique sur un décaissement

→ ouvre le détail.

Clique sur une échéance

→ ouvre son historique.

Clique sur un projet

→ ouvre automatiquement son espace.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4. LIENS ENTRE LES MODULES

Créer des liens logiques entre :

Tableau de bord

↔

Ma demande

↔

Mon dossier

↔

Mon chantier

↔

Notifications

↔

Support

Le Dashboard doit fonctionner comme une seule plateforme.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5. DONNÉES COHÉRENTES

Vérifier que :

une seule date est utilisée partout.

un seul statut est utilisé partout.

un seul numéro de dossier.

un seul numéro client.

une seule progression.

une seule liste de documents.

une seule liste de paiements.

une seule liste des décaissements.

Aucune incohérence ne doit être possible.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

6. GESTION DU CACHE

Mettre en place une stratégie de cache moderne.

Éviter les rechargements inutiles.

Actualiser uniquement les données modifiées.

Prévoir une invalidation intelligente du cache.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

7. ÉTAT GLOBAL

Créer une architecture de State Management propre.

Les modules doivent partager les mêmes données via un état global.

Éviter les états locaux dupliqués.

Créer des contextes spécialisés si nécessaire :

ClientContext

ProjetContext

DocumentsContext

ChantierContext

NotificationContext

PaiementContext

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

8. MISE À JOUR AUTOMATIQUE

Lorsqu'un Agent CPI valide un document :

→ le client voit immédiatement le changement.

Lorsqu'une photo est ajoutée au chantier :

→ elle apparaît immédiatement.

Lorsqu'un décaissement est validé :

→ le Dashboard se met à jour automatiquement.

Lorsqu'un paiement est effectué :

→ toutes les statistiques sont recalculées.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

9. GESTION DES ERREURS

Prévoir :

perte de connexion

erreur API

données incomplètes

doublons

conflits de mise à jour

tentative d'accès invalide

Afficher des messages clairs à l'utilisateur.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

10. PERFORMANCE

Réduire les appels réseau.

Mutualiser les requêtes.

Précharger les données utiles.

Charger les sections lourdes à la demande.

Optimiser les temps d'affichage.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

11. JOURNALISATION

Tracer automatiquement :

les modifications du profil

les dépôts de documents

les validations

les changements de statut

les paiements

les décaissements

les publications

Créer un historique fiable.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

12. QUALITÉ

Supprimer :

les données fictives inutiles

les incohérences

les variables non utilisées

les états morts

les redirections cassées

les composants orphelins

les données dupliquées

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OBJECTIF FINAL

Le Dashboard Client doit fonctionner comme une application SaaS moderne.

Le client ne doit jamais constater :

• une donnée incohérente

• un mauvais lien

• une information obsolète

• un écran qui nécessite un rafraîchissement

• une duplication d'informations

Toutes les pages doivent partager les mêmes données en temps réel et offrir une navigation fluide, cohérente et fiable.